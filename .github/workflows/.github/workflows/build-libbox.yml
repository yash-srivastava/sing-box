name: Build libbox.aar (Android AAR)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Ensure git and tools
        run: sudo apt-get update && sudo apt-get install -y git build-essential

      - name: Ensure tags (defensive)
        run: |
          if [ -f .git/shallow ]; then git fetch --unshallow || true; fi
          git fetch --tags --prune

      - name: Build libbox
        run: make lib_android

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: libbox-aar
          path: clients/android/app/libs/*.aar
